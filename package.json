{
    "name": "your-edu-interativo",
    "private": true,
    "version": "2.0.0",
    "type": "module",
    "description": "YouEdu - Intelligent Learning Platform with AI-powered assessments",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\"",
        "dev:web": "cd apps/web && npm run dev",
        "dev:api": "cd apps/api && python -m uvicorn main:app --reload --port 8000",
        "install:all": "npm install && cd apps/web && npm install && cd ../api && python -m venv venv && source venv/bin/activate && pip install -r requirements.txt",
        "build:web": "cd apps/web && npm run build",
        "preview:web": "cd apps/web && npm run preview"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}